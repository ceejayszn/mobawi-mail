{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "prisma generate && prisma db push --skip-generate --force-reset && node prisma/seed.js && next build",
  "framework": "nextjs"
}
